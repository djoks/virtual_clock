/// Log level for ClockService messages.
enum LogLevel {
  /// Informational message.
  info,

  /// Warning message (e.g., clock acceleration active).
  warning,

  /// Error message (e.g., invalid configuration).
  error,
}
