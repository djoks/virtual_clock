/// Color constants for the TimeControlPanel and related UI.
///
/// Colors are organized by theme (dark/light) and purpose.
library;

import 'dart:ui' show Color;

// =============================================================================
// Dark Theme Colors
// =============================================================================

/// Dark theme background color.
const kDarkBackground = Color(0xFF0A0A0B);

/// Dark theme secondary background color.
const kDarkBackgroundSecondary = Color(0xFF111113);

/// Dark theme hover background color.
const kDarkBackgroundHover = Color(0xFF1A1A1E);

/// Dark theme border color.
const kDarkBorder = Color(0x1F22C55E);

/// Dark theme accent color (green).
const kDarkAccent = Color(0xFF4ADE80);

/// Dark theme accent dimmed color (for backgrounds).
const kDarkAccentDim = Color(0x1A4ADE80);

/// Dark theme primary text color.
const kDarkTextPrimary = Color(0xFFFFFFFF);

/// Dark theme secondary text color.
const kDarkTextSecondary = Color(0xFFB0B0C0);

/// Dark theme tertiary text color.
const kDarkTextTertiary = Color(0xFF6B6B7B);

/// Dark theme reset button hover color.
const kDarkResetHover = Color(0xFFEF4444);

/// Dark theme reset button hover background color.
const kDarkResetHoverBg = Color(0x1AEF4444);

// =============================================================================
// Light Theme Colors
// =============================================================================

/// Light theme background color.
const kLightBackground = Color(0xFFF8F9FA);

/// Light theme secondary background color.
const kLightBackgroundSecondary = Color(0xFFFFFFFF);

/// Light theme hover background color.
const kLightBackgroundHover = Color(0xFFE9ECEF);

/// Light theme border color.
const kLightBorder = Color(0xFFDEE2E6);

/// Light theme accent color (green).
const kLightAccent = Color(0xFF22C55E);

/// Light theme accent dimmed color (for backgrounds).
const kLightAccentDim = Color(0x1A22C55E);

/// Light theme primary text color.
const kLightTextPrimary = Color(0xFF212529);

/// Light theme secondary text color.
const kLightTextSecondary = Color(0xFF6C757D);

/// Light theme tertiary text color.
const kLightTextTertiary = Color(0xFFADB5BD);

/// Light theme reset button hover color.
const kLightResetHover = Color(0xFFDC2626);

/// Light theme reset button hover background color.
const kLightResetHoverBg = Color(0x1ADC2626);
